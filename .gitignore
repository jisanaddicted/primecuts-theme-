# Shopify CLI local auth config (never commit)
.shopify/
**/.shopify/

# Editor / local tooling that is not part of the theme
.vscode/

# Local static preview helper (dev tooling, not uploaded to Shopify)
preview/

# Figma REST API scratch data (local design-import tooling)
.figma/

# Shopify CLI push logs (local tooling output, not part of the theme)
push_*.txt